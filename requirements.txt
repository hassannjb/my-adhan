# Web backend
fastapi>=0.115
uvicorn[standard]>=0.30

# RAG — offline, no API keys needed
sentence-transformers>=3.0
numpy>=1.26
ollama>=0.3

# Prayer time calculation (used by the chat tool)
adhanpy>=0.1.3
timezonefinder>=6.5
pytz>=2024.1
requests>=2.31
hijridate>=2.3
